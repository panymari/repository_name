import React from 'react';
import classes from './ErrorMessage.module.scss';

const ErrorMessage = () => {
  return <div className={classes.error}>Something was wrong!!!</div>;
};
export default ErrorMessage;
